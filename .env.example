# Copy this file to .env and fill values locally.
# Never commit real API keys.

VITE_AI_PROVIDER=mock
VITE_ASR_MODE=mock
VITE_ASR_MODEL=mock-streaming-asr

# Keep real provider keys in local-only runtime channels.
OPENAI_API_KEY=

# Future updates will wire these values into translation and optional TTS.
TRANSLATION_MODEL=
TTS_MODEL=
